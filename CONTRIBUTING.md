# Contributing

1. Fork the repo and create a branch from `main`.
2. Run `composer install` to install dependencies.
3. Write tests for any new behaviour (`composer test`).
4. Ensure static analysis passes (`composer analyse`).
5. Open a pull request — all CI checks must be green.
